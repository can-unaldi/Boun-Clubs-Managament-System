@model List<Event>
@{
    ViewData["Title"] = "Index";
}

<div class="bg-primary text-center">
    <h2>Onay Bekleyen Etkinlikler</h2>

</div>
<table class="table table-bordered table-striped ">
    <tr>
        <th>Etkinlik Adı</th>
        <th>Kulüp Adı</th>
        <th>Detaylar</th>
        <th>İtiraz Et</th>
    </tr>
    @if (Model.Count() == 0)
    {
        <tr>
            <td colspan="4" class="text-center">
                No Events
            </td>
        </tr>
    }
    else
    {
        foreach (var clubEvent in Model)
        {
            <tr>
                <td>@clubEvent.EventName</td>
                <td>@clubEvent.ClubName</td>
                <td>
                    <a asp-action="Details" asp-controller="Event" asp-route-id="@clubEvent.EventId" class="btn btn-primary">
                        Detaylar
                    </a>
                </td>
                <td>
                    <a href="mailto:@ViewData["clubmail"]?cc=@ViewData["kakmail"]&subject=@ViewData["eventname"]%20Hakkında%20İtiraz" class="btn btn-danger">
                        İtiraz Et
                    </a>
                </td>
            </tr>
        }
    }
</table>

